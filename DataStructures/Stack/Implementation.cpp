#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;


class stImpl{
    int top = -1;
    int stack[10];

    void push(int x){
        if (top>=10){
            top = top + 1;
            stack[top] = x

}
    }
};

int main(){

    return 0;
}